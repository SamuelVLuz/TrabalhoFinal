<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrabalhoFinal"
        tools:targetApi="31">
        <activity
            android:name=".RelatorioEvento"
            android:exported="false" />
        <activity
            android:name=".RegistrarPresenca"
            android:exported="false" />
        <activity
            android:name=".UsuarioDetalhes"
            android:exported="false" />
        <activity
            android:name=".EventoDetalhes"
            android:exported="false" />
        <activity
            android:name=".ListaUsuarios"
            android:exported="false" />
        <activity
            android:name=".CadastrarUsuarios"
            android:exported="false" />
        <activity
            android:name=".CadastrarEventos"
            android:exported="false" />
        <activity
            android:name=".ListaEventos"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/fileprovider_paths" />
        </provider>
    </application>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

</manifest>